/* BAA M48 — authenticated global collaboration client. */
(function(global){'use strict';
 async function api(method,body){const r=await fetch('/api/m48-collaboration.js',{method,headers:{'Content-Type':'application/json'},body:body?JSON.stringify(body):undefined});const d=await r.json();if(!r.ok)throw new Error(d?.error?.message||'Collaboration request failed.');return d;}
 async function list(){return api('GET');}
 function mount(root){if(!root)return;root.innerHTML='<section class="baa-collaboration-card"><h2>🌍 Global Student Collaboration</h2><p data-status>Share learning questions and discoveries with the BAA community.</p><form data-post><input name="title" required maxlength="180" placeholder="Discussion title"><textarea name="body" required maxlength="4000" placeholder="What would you like to share?"></textarea><input name="subject" maxlength="120" placeholder="Subject (optional)"><button type="submit">Publish</button></form><div data-feed></div></section>';const feed=root.querySelector('[data-feed]'),status=root.querySelector('[data-status]');async function refresh(){try{const d=await list();feed.innerHTML=d.posts.map(p=>`<article><strong>${p.title}</strong><small> · ${p.authorName||'BAA learner'}</small><p>${p.body}</p><button data-comment="${p.id}">Comment</button></article>`).join('')||'<p>No discussions yet.</p>';}catch(e){status.textContent=e.message;}}root.querySelector('[data-post]').addEventListener('submit',async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget));try{await api('POST',{action:'post',...b});e.currentTarget.reset();status.textContent='Published.';refresh();}catch(err){status.textContent=err.message;}});refresh();}
 global.BAACollaboration={api,list,mount};if(typeof document!=='undefined')document.querySelectorAll('[data-baa-collaboration]').forEach(mount);
})(window);
