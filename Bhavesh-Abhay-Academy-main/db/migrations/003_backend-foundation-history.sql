-- BAA migration 003 — historical numbering marker.
-- Backend foundation work was incorporated into the canonical schema and
-- subsequent numbered migrations. This marker intentionally performs no
-- destructive or duplicate DDL; it closes the historical 001 -> 004 gap.
SELECT 1;
